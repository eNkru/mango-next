# Host port published to the container (container always listens on 9000).
PORT=9000

# Path to the mango main directory (database, library, uploads, plugins, queue).
MAIN_DIRECTORY_PATH=./data

# Path to the mango config directory (config.yml is created on first run).
CONFIG_DIRECTORY_PATH=./config
